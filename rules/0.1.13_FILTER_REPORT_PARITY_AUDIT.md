# OpenGLESScope Database 0.1.13 filter/report parity audit

- Report index date placement, sort controls, per-page selector and pagination were aligned to the reference database hierarchy.
- Server-submitted timestamps remain authoritative and are localized only for presentation.
- Every main destination was checked for meaningful filter visibility. Display/HDR does not inherit graphics filters.
- Extension scope, runtime-format family and shader-stage selectors were added without changing evidence semantics.
- Status menus are view-specific and do not offer unsupported states that the normalized OpenGL ES/EGL data cannot justify.
- Coverage outside-fill emphasis follows the unique-dominant rule and ties remain neutral.
- Footer top divider and compact geometry were restored without root horizontal overflow.
