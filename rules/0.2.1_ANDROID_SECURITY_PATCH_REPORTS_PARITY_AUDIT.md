# OpenGLESScope Database 0.2.1 Android security-patch Reports parity audit

- Reports Android cells match VulkanScope ordering and typography when explicit patch evidence exists: Android release/SDK first, `Patch YYYY-MM-DD` second in `table-sub` styling.
- Schema 2 accepts optional `device.securityPatch` only in canonical ISO calendar-date text form and keeps all prior required device fields unchanged.
- Older reports without security-patch evidence remain valid and do not display guessed values.
- Worker runtime metadata exposes only submitted/TXT-backed patch evidence.
- OpenGL ES/EGL capability semantics and Display/HDR evidence are unchanged.
- Static audit, JavaScript syntax, Worker contract tests and ZIP integrity are release gates.
