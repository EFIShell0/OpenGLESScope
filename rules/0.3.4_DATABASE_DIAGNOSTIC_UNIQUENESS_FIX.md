# OpenGLESScope 0.3.4 Database diagnostic uniqueness fix

0.3.3 could emit duplicate diagnostic names because several count queries were collected once for Limits and queried again for enumeration. Database correctly rejects duplicate diagnostics. 0.3.4 keeps one canonical diagnostic record per query name and conservatively merges repeated internal reads, while preserving separate count and array query names.
