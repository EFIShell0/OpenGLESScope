# OpenGLESScope Database 0.1.20 Responsive Table Audit

## Result
PASS

## Verified behavior
- Moving the native table scroll position moves the custom thumb.
- Thumb width tracks visible table fraction.
- Pointer dragging the track changes the table scroll position.
- Arrow/Home/End keyboard control changes the same scroll position.
- Left/right edge shadows appear only while content remains hidden in that direction.
- Arrow disabled state follows the same start/end calculation.
- Controls disappear when no horizontal overflow exists.
- Resize and dynamically inserted tables are resynchronized.
- Reduced motion preserves function while removing nonessential transitions.

## Scope preservation
No application producer contract, OpenGL ES/EGL evidence, D1 schema or canonical report payload is changed.
