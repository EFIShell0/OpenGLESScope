# OpenGLESScope Database 0.7.0 full correctness, security and EGL audit

## Producer contract

- Database version is 0.7.0.
- Current producer is OpenGLESScope 0.7.0 / versionCode 700.
- Submission envelope remains schema 2.
- OpenGLESScope 0.7.0 requires technicalReport schema 2.
- Compatible historical producers retain technicalReport schema 1 without rewriting stored reports.
- Normalizer version is 10.

## EGL validation

- Current technicalReport schema 2 requires the structured `eglRuntime` object.
- Current EGL runtime binding evidence is fail-closed for current context/display/draw/read-surface consistency.
- EGL Config extension fields are accepted only when the corresponding EGL extension token is present.
- Per-config and runtime unavailable-attribute evidence is bounded and validates exact name/error structure.
- No EGL or OpenGL ES capability is inferred from vendor identity.

## Compare and report detail

- Report Detail exposes EGL runtime and expanded EGL Config evidence.
- Compare preserves semantic status and exact canonical evidence identity.
- Common evidence only and cross-producer warnings remain available.
- One-sided absence remains Unknown / Not reported and is never fabricated as Unsupported.

## Security and storage

- 2 MiB request/report bounds, recursive sensitive-field rejection, stable canonical JSON hashing and parameterized D1 access remain mandatory.
- CORS remains pinned to the official Pages origin.
- Current browser assets are app.v070.js and site.v070.css; stale versioned assets are release blockers.
- Pages deploys only the allow-listed staged artifact and must exclude Worker source, tools, rules, workflows and transients.
- No D1 migration, report rewrite or report-hash rewrite is introduced by 0.7.0.

## Release gates

Source audit, canonical repository-state check, JavaScript syntax, hash-route contract, Compare contract, Worker contract, audit-hygiene regression and allow-listed Pages artifact audit must all pass before release packaging.
