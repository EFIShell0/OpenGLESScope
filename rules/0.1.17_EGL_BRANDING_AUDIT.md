# OpenGLESScope Database 0.1.17 EGL Branding Audit

- Official EGL artwork supplied for the project is bundled locally as `assets/egl-logo-v027.png` with transparency and original aspect ratio preserved.
- The primary EGL navigation destination uses the official EGL logo instead of the former generic semantic SVG.
- The EGL main-view heading and report-detail EGL tab use the same official artwork at compact sizes consistent with the surrounding OpenGLESScope UI.
- EGL Configs retains its distinct configuration icon because it represents a technical configuration table rather than the EGL runtime destination itself.
- No remote image dependency was introduced.
- OpenGL ES/EGL report semantics, D1 schema, filters, counts, report normalization and canonical TXT access are unchanged.
- Frontend assets are cache-busted to v027.
