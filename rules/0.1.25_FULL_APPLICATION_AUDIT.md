# OpenGLESScope 0.1.25 Full Application Audit

PASS: OpenGLESScope 0.1.25 remains compatible with OpenGLESScope Database 0.1.21 exact schema-v2 / technicalReport-v1 submission rules.

PASS: Canonical TXT and HTML retain Android version, SDK, Application ABI and Supported device ABIs. Database 0.1.21 can derive runtime ABI metadata without a schema mutation.

PASS: Khronos baseline audit uses OpenGL ES 3.2, GLSL ES 3.20 and EGL 1.5 as the current core specifications.

PASS: The graphics-relevant external OpenGL ES capability reference comparison floor is covered: runtime versions, exact extensions, compressed formats, shader/program binary formats, implementation limits and EGL implementation data. OpenGLESScope additionally exposes ES 3.1/3.2 limits, EGL configurations, per-query diagnostics and Android Display/HDR evidence. Sensors and unrelated Android inventory remain intentionally excluded by PROJECT_RULES.md.

PASS: Pre-3.2 GL_KHR_debug limit queries are exact-extension gated.

PASS: GL_EXT_disjoint_timer_query TIME_ELAPSED_EXT and TIMESTAMP_EXT counter-bit queries are exact-extension gated and entry-point failures remain explicit diagnostics.

PASS: Query additions automatically flow through the common limits/diagnostics model into UI, TXT, HTML and Database technicalReport payloads.

PASS: No Database schema, Worker, D1 migration, upload policy, ABI policy or endpoint was changed by this application-only release.
