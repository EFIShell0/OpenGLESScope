# OpenGLESScope Database 0.1.24 UI Parity Fix Audit

Reference: VulkanScope Database 0.35.8.

- Main navigation button and arrow-control geometry aligned to the reference.
- Reports toolbar filter/select geometry aligned to the reference.
- Driver mode cells preserve labels such as System driver on one line.
- Display/HDR available luminance values append cd/m² without changing unavailable/unknown state semantics.
- Native table horizontal scrollbar is visually suppressed, leaving the synchronized custom scrollbar as the single visible table scrollbar.
- OpenGL ES, EGL, Android Display/HDR, submission, normalization and security semantics are unchanged.
- Changed browser assets are cache-busted as app.v034.js and site.v034.css.
