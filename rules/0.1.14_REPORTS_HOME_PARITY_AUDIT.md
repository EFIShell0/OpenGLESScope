# OpenGLESScope Database 0.1.14 Reports/Home parity audit

## Scope
- Removed Overview from the primary navigation and default route.
- Made Reports the root/default destination.
- Re-audited report-index cursor loading and visible pagination against the VulkanScope Database quality reference.

## Required behavior
- API index batch: 500 summaries per cursor request.
- Cursor traversal: continue until `nextCursor` is absent; repeated cursor fails visibly.
- Visible report page sizes: 10, 25, 50 only; default 25; hard maximum 50.
- Search/filter/sort execute before visible pagination.
- Submission timestamp is server-authored and rendered with date, time, seconds and timezone.
- Reports is the bare product document title.
- OpenGL ES/EGL evidence semantics are unchanged.

## Static audit result
PASS
